# Websquire Custom Site Form

Read `AGENTS.md` first: this repo is an Ockno-rendered site with two hard rules (no scripts in pages, keep every `ockno-page` meta) and a small vocabulary of `data-ockno-*` attributes for forms, booking and hosted embeds.
